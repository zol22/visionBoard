<img src={getThesource(imageGrid,index)} className='w-full object-cover'></img>


{imageGrid && index === currentButtonIndex ? (
    <div>
        {imageGrid.map((image,i) => (
            <img key={i} src={image.src}></img>
        ))}

    </div> ): (
    <div className='mb-4'>
        <img src={topics.src} className='w-full object-cover'></img>
    </div>
)}

idArrays.map(ids => ids.includes(imageGrid.map(image=>image.id)))
imageGrid: [
    {
        id: '3',
        src: ''
    }
]


topics = [
    {
        src: '',
        title: '',
        description: '',
        id: '3'
    }
]


display all images from topics src which id's are not found in imageGrid, for example image id:3 is not displayed 
otherwisewith the new source; 